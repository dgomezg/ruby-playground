<h1>Products#index</h1>

<%= link_to "New Product", new_product_path %>

<div id="products">
  <% @products.each do | product| %>
    <div>
      <!--
      <a href="/products/<%= product.id %>">
        <%= product.name %>
      </a>
      -- previous lines are equivalent to the following one
      -->
      <%= link_to product.name, product %>
    </div>
  <% end %>
</div>

<hr/>
<%= debug @products %>
